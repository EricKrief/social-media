interface UserItemProps {
  firstName: string;
  lastName: string;
  pictureUrl: string;
}

export const UserItem = ({ firstName, lastName, pictureUrl }: UserItemProps) => {

  return (
    <div></div>
  );
}